 services:
   app:
    container_name: Trab_api
    ports:
    - 3000:3000
    build:
      context: .
      dockerfile: Dockerfile.${NODE_ENV}
    env_file: .env
    environment:
     - NODE_ENV=${NODE_ENV}
     - port=${port}